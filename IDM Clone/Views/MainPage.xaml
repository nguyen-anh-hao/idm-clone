<Page
    x:Class="IDM_Clone.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:IDM_Clone.Models"
    xmlns:local="using:IDM_Clone.Views"
    xmlns:ViewModels="using:IDM_Clone.ViewModels"
    mc:Ignorable="d">

    <!-- Page Resources -->

    <Page.Resources>
        <DataTemplate x:Key="DownloadItemTemplate" x:DataType="model:DownloadItem">
            <Border Width="760" Height="Auto" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid Padding="24" ColumnSpacing="32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE8A5;" FontSize="32" Grid.Column="0" />
                    <StackPanel Grid.Column="1" Spacing="16">
                        <TextBlock 
                            TextWrapping="Wrap" 
                            IsTextSelectionEnabled="True"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Text="{x:Bind Name}" 
                            HorizontalAlignment="Stretch" />
                        <TextBlock
                            Margin="0 0 0 8"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            TextWrapping="Wrap"
                            Text="{x:Bind Status, Mode=OneWay}"
                            Name="DownloadStatus" />
                        <TextBlock
                            Margin="0 0 0 8"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            TextWrapping="Wrap"
                            Text="{x:Bind DownloadedPartSize, Mode=OneWay}"
                            Name="DownloadedPartSize" />
                        <ProgressBar Value="{x:Bind Progress, Mode=OneWay}" Name="ProgressBar" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                        <Button Name="ShowInFolder" Click="ShowInFolder_Click">
                            <Button.Content>
                                <FontIcon Glyph="&#xF12B;" FontSize="16" />
                            </Button.Content>
                        </Button>
                        <Button Name="CopyURL" Click="CopyURL_Click">
                            <Button.Content>
                                <FontIcon Glyph="&#xE71B;" FontSize="16" />
                            </Button.Content>
                        </Button>
                        <Button>
                            <Button.Content>
                                <FontIcon Glyph="&#xE712;" FontSize="16" />
                            </Button.Content>
                            <Button.Flyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Tạm dừng" />
                                    <MenuFlyoutItem Text="Hủy bỏ" />
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <StackPanel Orientation="Vertical" MaxWidth="768" Spacing="8">
        <Grid ColumnSpacing="8" Margin="0 0 0 40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox PlaceholderText="Nhập URL" Grid.Column="0" Name="URL" />
            <Button Content="Tải xuống" Grid.Column="1" Name="Download" Click="Download_Click"/>
        </Grid>

        <!-- Download List -->
        <GridView ItemsSource="{x:Bind ViewModel.Downloads, Mode=TwoWay}" ItemTemplate="{StaticResource DownloadItemTemplate}">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Margin" Value="4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="GridViewItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </GridView.ItemContainerStyle>
        </GridView>
    </StackPanel>
</Page>
